<!--index.wxml-->
<view class="swiper-tab">  
    <view class="swiper-tab-list {{currentTab==0 ? 'on' : ''}}" data-current="0" bindtap="swichNav">全部</view>
    <view class="swiper-tab-list {{currentTab==2 ? 'on' : ''}}" data-current="2" bindtap="swichNav">本地</view>
    <view class="swiper-tab-list {{currentTab==1 ? 'on' : ''}}" data-current="1" bindtap="swichNav">科技</view>
    <!--
    <view class="swiper-tab-list {{currentTab==2 ? 'on' : ''}}" data-current="2" bindtap="swichNav">娱乐</view>
    <view class="swiper-tab-list {{currentTab==2 ? 'on' : ''}}" data-current="2" bindtap="swichNav">体育</view>
    <view class="swiper-tab-list {{currentTab==2 ? 'on' : ''}}" data-current="2" bindtap="swichNav">财经</view>
    <view class="swiper-tab-list {{currentTab==2 ? 'on' : ''}}" data-current="2" bindtap="swichNav">历史</view>
    <view class="swiper-tab-list {{currentTab==2 ? 'on' : ''}}" data-current="2" bindtap="swichNav">房产</view>-->
</view>


<swiper current="{{currentTab}}" class="swiper-box" duration="300" style="height:{{winHeight - 31}}px" bindchange="bindChange">  
    <swiper-item>  
      <view class="tunnel">
          <view class="tunnel-text uploader-text">
              <text bindtap="doRequest">UserInfo can be retrieved from Weixin:</text>
          </view>
          <view class="tunnel-text" wx:if="{{logged}}">
              <text class="request-text">{{'Gender: ' + userInfo.gender}}</text>
          </view>
          <view class="tunnel-text" wx:if="{{logged}}">
              <text class="request-text">{{'Province: ' + userInfo.province}}</text>
          </view>
      </view>
    </swiper-item>  

    <swiper-item>  
      <view>ToDo</view>  
    </swiper-item>  

    <swiper-item>  
      <view>ToDo</view>  
    </swiper-item>  
</swiper>